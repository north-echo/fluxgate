stages:
  - build

build:
  stage: build
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
  script:
    - git fetch origin
    - git checkout $CI_MERGE_REQUEST_SOURCE_BRANCH_NAME
    - npm install
    - npm run build
