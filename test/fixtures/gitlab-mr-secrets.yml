stages:
  - test
  - deploy

variables:
  DEPLOY_TOKEN: $CI_JOB_TOKEN

test:
  stage: test
  tags:
    - docker
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
  script:
    - pip install -r requirements.txt
    - pytest tests/
