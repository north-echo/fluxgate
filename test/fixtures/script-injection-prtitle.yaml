name: PR Title Injection
on:
  pull_request_target:
    types: [opened]
jobs:
  label:
    runs-on: ubuntu-latest
    steps:
      - name: Use title
        run: |
          TITLE="${{ github.event.pull_request.title }}"
          grep "$TITLE" data.txt
