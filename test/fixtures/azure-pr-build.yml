trigger:
  branches:
    include:
      - main

pr:
  branches:
    include:
      - main

pool:
  vmImage: 'ubuntu-latest'

variables:
  - group: production-secrets

steps:
  - checkout: self
  - script: npm ci
    displayName: Install dependencies
  - script: npm test
    displayName: Run tests
