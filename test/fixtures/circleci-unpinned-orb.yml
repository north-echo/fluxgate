version: 2.1

orbs:
  node: circleci/node@5.0
  slack: circleci/slack@4.12.0

jobs:
  build:
    docker:
      - image: cimg/node:18.17
    steps:
      - checkout
      - run:
          name: Build
          command: npm run build

workflows:
  main:
    jobs:
      - build
